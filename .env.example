ENV=development
PORT=8001
MODEL_DIR=./training/models

# LLM explainer — if missing, backend runs in mock mode (WIO still works)
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Optional data sources — all optional, decoders use fixtures if missing
IMD_API_BASE=https://api.data.gov.in
IMD_API_KEY=
OPENWEATHER_API_KEY=
HF_TOKEN=

# Kaggle / training
WANDB_MODE=disabled
