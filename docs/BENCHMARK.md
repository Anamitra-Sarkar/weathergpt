# Benchmark

No benchmark numbers are claimed yet. The API exposes per-process WIO and RADE mean latency through `GET /metrics`; run a controlled cold- and warm-cache benchmark against a live deployment before publishing latency figures.
