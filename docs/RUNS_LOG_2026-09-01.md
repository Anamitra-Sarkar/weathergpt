# Kaggle Runs Log — 2026-09-01

All runs on `arkosarkarhehe` (private), `enable_gpu true`, `internet true`, no local data download.

| Version | Kernel | GPU (actual) | Status at pack-up | Key Output |
|---------|--------|--------------|-------------------|------------|
| v1 p100-train | `weathergpt-p100-train` | P100 16GB `cap 6,0` | COMPLETE | `field_names 1500 dup 1269`, `matched_pairs 4800 corr 1.0` duplicate (no `models=gfs_seamless`) |
| checker | `weathergpt-dataset-checker` | P100 | COMPLETE | `report.json` `M1 NEEDS (dup, label, TMAX, acc)`, `M2 NEEDS (lead 0-239)`, `M3 86% none`, `force CPU` |
| v2 p100-train | `weathergpt-p100-train` | P100 → CPU | COMPLETE | `M1 165` pad bug, `M1 TF-IDF acc 1.0` overfit, `M2 4800` fixed to `corr 0.96` via `gfs_seamless` |
| official v1 | `weathergpt-official` | P100 → CPU | COMPLETE | `field_names 131` (pad bug), `matched_pairs 14400`, `intent 2000 template`, `M1/M3 tokenizer bug` → `M2 best 1.01 rmse_t 1.34` only |
| official v2 | `weathergpt-official` | P100 → CPU | COMPLETE | Same 131 rows, `eval_strategy` fixed but `tokenizer` still bug |
| official v3 | `weathergpt-official` | P100 → CPU | COMPLETE | `field_names 131→1200` pad still 131, `M1/M3 tokenizer` still bug |
| official v4 | `weathergpt-official` | **RUNNING** at 01:45 IST | RUNNING | `M1 1200 unique 1200 per_label 365/155…`, `M2 14400 lead %72`, `M3 2000 Groq 800/800` → DistilBERT 9/5-label with `no_cuda=force_cpu` fix |

**Sample log evidence (official v2 tail):**
```
M1 training/datasets/field_names.csv rows=131 unique=131 per_label=Counter({'precipitation_amount': 56, ...})
M2 written 14400 lead 0-71 verified [0,1,2,3,4]
M1 labels 9 rows 131
Traceback: Trainer.__init__() got unexpected keyword argument 'tokenizer'
M2 split train 12240 val 2160 … best 0.922 rmse_t 1.34
M3 failed same tokenizer
OFFICIAL DONE: M1 False M2 True M3 False
```

**Sample log evidence (T4 v3 40.5s):**
```
M1 written rows=165 unique=165 per_label 69 precip …
M1 TF-IDF acc 1.000 f1 1.000
M2 split 4080/720 rmse_t 1.27 rmse_p 0.046 best 0.817
M3 acc 0.456 f1 0.454
v3 done: M1 True M2 True M3 True
```

**Artifacts (last COMPLETE official v2):**
* `training/datasets/field_names.csv` 132 lines, `matched_pairs.csv` 14401 lines, `intent_samples.jsonl` 2000 lines → 16533 total
* `training/models/bias_correction/best.pt 104K`, `scaler.pkl 570B`, `metrics.json {"best_val_loss":0.922, "rmse_t":1.34, "rmse_p":0.20, "rows":14400}`
* No `semantic_classifier` / `intent_parser` models yet (failed) — v4 will produce them.

**Groq 4-model queue (live 200 on all):**
`qwen/qwen3.8-27b` (orchestrator) → `qwen/qwen3.6-27b` → `openai/gpt-oss-20b` → `openai/gpt-oss-120b`, round-robin for 6 agents, llama rejected. `M3 Groq 0/800 … 700/800` logs confirm queue working, but v2 had `Groq ready: False` (key not injected) → v4 hard-injected.

**P100 vs T4:**
* P100 `cap 6,0` needs `sm_70+` → all v1–v4 correctly forced `device=cpu` (`M2` trains on CPU, `M1/M3` now `no_cuda=True`)
* T4 `cap 7,5` would use `device=cuda` + `fp16` + `DataParallel` on 2 GPUs — checker showed `force CPU` on P100 is expected, not a bug.
